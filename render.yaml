services:
  - type: web
    name: taskmanager-web
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: |
      gunicorn taskmanagerproject.wsgi:application --log-file -
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "0"
      - key: ALLOWED_HOSTS
        value: "taskmanager-web.onrender.com"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://taskmanager-web.onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: taskmanager-db
          property: connectionString

databases:
  - name: taskmanager-db
    plan: free
